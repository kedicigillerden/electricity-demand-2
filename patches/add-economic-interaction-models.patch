diff --git a/talep_tahmin_tek_dosya.py b/talep_tahmin_tek_dosya.py
--- a/talep_tahmin_tek_dosya.py
+++ b/talep_tahmin_tek_dosya.py
@@
-TARGET_YEARS = [2022, 2023, 2024]
-ALL_CONTINUOUS_COLUMNS = ["HDD", "CDD", "PMI", "IR", "CUR"]
+TARGET_YEARS = [2022, 2023, 2024]
+ALL_CONTINUOUS_COLUMNS = [
+    "HDD",
+    "CDD",
+    "PMI",
+    "IR",
+    "CUR",
+    "PMI_c",
+    "IR_c",
+    "CUR_c",
+    "PMI_IR",
+    "CUR_IR",
+    "PMI_CUR",
+]
 BINARY_COLUMNS = ["weekend", "night"]
@@
     econ = load_economic_data()
     df = df.merge(econ, on="year_month", how="left")
+    for column in ("PMI", "IR", "CUR"):
+        df[f"{column}_c"] = df[column] - df[column].mean()
+    df["PMI_IR"] = df["PMI_c"] * df["IR_c"]
+    df["CUR_IR"] = df["CUR_c"] * df["IR_c"]
+    df["PMI_CUR"] = df["PMI_c"] * df["CUR_c"]
     return df
@@
         {
             "model_id": "model_8",
             "prompt": "TABLE 8 - sqrt_consumption ~ HDD + CDD + night dummy + weekend dummy + PMI + IR",
             "y_col": "sqrt_consumption",
             "features": ["HDD", "CDD", "night", "weekend", "PMI", "IR"],
             "use_month_dummies": False,
         },
+        {
+            "model_id": "model_9",
+            "prompt": "TABLE 9 - consumption ~ HDD + CDD + night dummy + weekend dummy + PMI_c + IR_c + PMI_IR",
+            "y_col": "consumption",
+            "features": [
+                "HDD",
+                "CDD",
+                "night",
+                "weekend",
+                "PMI_c",
+                "IR_c",
+                "PMI_IR",
+            ],
+            "use_month_dummies": False,
+        },
+        {
+            "model_id": "model_10",
+            "prompt": "TABLE 10 - consumption ~ HDD + CDD + night dummy + weekend dummy + PMI_c + IR_c + CUR_c + PMI_IR + CUR_IR",
+            "y_col": "consumption",
+            "features": [
+                "HDD",
+                "CDD",
+                "night",
+                "weekend",
+                "PMI_c",
+                "IR_c",
+                "CUR_c",
+                "PMI_IR",
+                "CUR_IR",
+            ],
+            "use_month_dummies": False,
+        },
+        {
+            "model_id": "model_11",
+            "prompt": "TABLE 11 - consumption ~ HDD + CDD + night dummy + weekend dummy + PMI_c + IR_c + CUR_c + PMI_IR + CUR_IR + PMI_CUR",
+            "y_col": "consumption",
+            "features": [
+                "HDD",
+                "CDD",
+                "night",
+                "weekend",
+                "PMI_c",
+                "IR_c",
+                "CUR_c",
+                "PMI_IR",
+                "CUR_IR",
+                "PMI_CUR",
+            ],
+            "use_month_dummies": False,
+        },
     ]
